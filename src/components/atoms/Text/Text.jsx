import React from "react"
import styled from "@emotion/styled"

const Text = styled.div`
  color: blue;
`

Text.propTypes = {
  // bla: PropTypes.string,
}

Text.defaultProps = {
  // bla: 'test',
}

export default Text
