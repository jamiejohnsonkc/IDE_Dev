]import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Slate.styles';

const Slate = (props) => (
  <div className="SlateWrapper">
    Test content
  </div>
);

Slate.propTypes = {
  // bla: PropTypes.string,
};

Slate.defaultProps = {
  // bla: 'test',
};

export default Slate;
